<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ランニング計測画面のGPS実測（geolocator）に必要な位置情報権限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- 計測中に画面を消してもGPSの取得が止まらないようにするための
         フォアグラウンドサービス。geolocator が位置ストリームの購読中だけ
         起動し、常駐通知を表示する。Android 14 (API 34) 以降は
         FOREGROUND_SERVICE_LOCATION の宣言が必須。
         サービスはユーザーが「スタート」を押した時点＝アプリが前面にある
         状態から開始するため ACCESS_BACKGROUND_LOCATION は不要
         （Google Play のバックグラウンド位置情報の用途申請も発生しない）。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- 画面消灯中もCPUを起こしたまま測位を続けるため
         （ForegroundNotificationConfig.enableWakeLock = true に対応）。 -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- 走破・地図コレクション画面の地図タイル取得（maplibre_gl）、Firestore等に必要 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="KOKUDO"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
